// import font
import { Inter } from "next/font/google";

export const font = Inter({
  variable: "--font",
  subsets: ["latin"],
  weight: "variable",
  axes: ["opsz"],
  style: "normal",
  display: "swap",
});
